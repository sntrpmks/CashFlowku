<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorBg">

    <!-- Top AppBar -->
    <RelativeLayout
        android:id="@+id/layout_header_backup"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@color/colorPrimary"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/btn_back_backup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:text="←"
            android:textColor="@color/colorOnPrimary"
            android:textSize="24sp"
            android:textStyle="bold"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toEndOf="@id/btn_back_backup"
            android:layout_marginStart="16dp"
            android:text="Backup &amp; Restore"
            android:textColor="@color/colorOnPrimary"
            android:textSize="18sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <!-- Cloud backup visual card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_backup_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="3dp"
        app:cardBackgroundColor="@color/colorCardBg"
        app:layout_constraintTop_toBottomOf="@id/layout_header_backup">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp"
            android:gravity="center">

            <!-- Local storage bubble graphic -->
            <TextView
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:background="@android:drawable/toast_frame"
                android:backgroundTint="#1010B981"
                android:gravity="center"
                android:text="💾"
                android:textSize="32sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:text="Penyimpanan Cadangan Database"
                android:textColor="@color/textPrimary"
                android:textSize="16sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_backup_connection_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="Cadangan Lokal (Offline) Aktif"
                android:textColor="@color/success"
                android:textSize="13sp" />

            <!-- Last sync time -->
            <TextView
                android:id="@+id/tv_last_backup_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Terakhir sinkronisasi: Belum pernah"
                android:textColor="@color/textSecondary"
                android:textSize="12sp" />

            <!-- Buttons -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="20dp"
                android:orientation="horizontal">

                <Button
                    android:id="@+id/btn_action_backup"
                    style="@style/Widget.CashFlowku.Button"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginEnd="6dp"
                    android:text="Backup Baru" />

                <Button
                    android:id="@+id/btn_action_restore"
                    style="@style/Widget.CashFlowku.Button.Secondary"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="6dp"
                    android:text="Restore Data" />
            </LinearLayout>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

    <!-- Logs History Title -->
    <TextView
        android:id="@+id/tv_logs_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="20dp"
        android:text="Histori Backup &amp; Restore"
        android:textColor="@color/textPrimary"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/card_backup_status" />

    <!-- Logs recycler -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_backup_logs"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:clipToPadding="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_logs_title"
        tools:listitem="@layout/item_backup_log" />

    <!-- Empty state -->
    <TextView
        android:id="@+id/tv_empty_backup_logs"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:gravity="center"
        android:text="Belum ada riwayat backup database."
        android:textColor="@color/textSecondary"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/tv_logs_title" />

</androidx.constraintlayout.widget.ConstraintLayout>
